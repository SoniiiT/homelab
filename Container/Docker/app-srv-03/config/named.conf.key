# Use "tsig-keygen -a hmac-sha256" and paste it in here or in Docker sudo docker exec -it bind9 sh -c "tsig-keygen -a hmac-sha256"
key "tsig-key" {
        algorithm hmac-sha256;
        secret "YourTSIGKeyHere!";
};