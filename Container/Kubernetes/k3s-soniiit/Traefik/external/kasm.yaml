apiVersion: v1
kind: Service
metadata:
  name: kasm
  namespace: traefik-external-kasm
spec:
  type: ExternalName
  externalName: 192.168.178.45
  ports:
    - port: 443
      targetPort: 443
      protocol: TCP
      name: https
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kasm
  namespace: traefik-external-kasm
  annotations: 
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`kasm.soniiit.net`)
      kind: Rule
      services:
        - name: kasm
          namespace: traefik-external-kasm
          port: 443
          scheme: https
  tls:
    secretName: soniiit-net-production-tls
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: soniiit-net-production-kasm
  namespace: traefik-external-kasm
spec:
  secretName: soniiit-net-production-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - "kasm.soniiit.net"
