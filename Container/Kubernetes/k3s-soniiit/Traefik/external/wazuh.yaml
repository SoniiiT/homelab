apiVersion: v1
kind: Service
metadata:
  name: wazuh
  namespace: traefik-homelab
spec:
  type: ExternalName
  externalName: 192.168.178.49
  ports:
    - port: 443
      targetPort: 443
      protocol: TCP
      name: https
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wazuh
  namespace: traefik-homelab
  annotations: 
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`wazuh.home.soniiit.net`)
      kind: Rule
      services:
        - name: wazuh
          namespace: traefik-homelab
          port: 443
          scheme: https
  tls:
    secretName: home-soniiit-net-selfsigned-tls
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-soniiit-net-selfsigned-wazuh
  namespace: traefik-homelab
spec:
  secretName: home-soniiit-net-selfsigned-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
    - "wazuh.home.soniiit.net"
